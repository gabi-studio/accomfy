console.log("Login page JS loaded.");

// You could expand this for login validation or modal toggles
document.querySelector("form").addEventListener("submit", function(e) {
  e.preventDefault();
  alert("Login submitted (this is a placeholder)");
});
